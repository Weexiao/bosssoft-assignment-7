<<<<<<< HEAD
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.springcloudprovider8081.dao.PermissionMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.example.springcloudprovider8081.entity.po.PermissionPO">
        <id column="id" property="id" />
        <result column="label" property="label" />
        <result column="parent_id" property="parentId" />
        <result column="parent_name" property="parentName" />
        <result column="code" property="code" />
        <result column="path" property="path" />
        <result column="name" property="name" />
        <result column="url" property="url" />
        <result column="type" property="type" />
        <result column="icon" property="icon" />
        <result column="create_time" property="createTime" />
        <result column="update_time" property="updateTime" />
        <result column="remark" property="remark" />
        <result column="order_num" property="orderNum" />
        <result column="is_delete" property="isDelete" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, label, parent_id, parent_name, code, path, name, url, type, icon, create_time, update_time, remark, order_num, is_delete
    </sql>

    <!--根据用户ID查找权限列表-->
    <select id="findPermissionListByUserId" resultMap="BaseResultMap">
        select
            DISTINCT
            p.id, p.parent_id, p.label, p.`code`, p.url, p.type, p.icon, p.remark, p.path, p.name
        from
            sys_user as u
        left join sys_user_role as ur on u.id = ur.user_id
        left join sys_role as r on ur.role_id = r.id
        left join sys_role_permission as rp on rp.role_id = r.id
        left join sys_permission as p on rp.permission_id = p.id
        where
            u.id = #{userId}
        order by p.id asc
    </select>

    <select id="findPermissionListByRoleId" resultMap="BaseResultMap">
        select
            p.id, p.parent_id, p.label, p.parent_name, p.`code`, p.url, p.type, p.icon, p.remark, p.path, p.name,
            p.create_time, p.update_time, p.order_num, p.is_delete
        from
            sys_permission as p
        inner join sys_role_permission as rp on p.id = rp.permission_id
        where
            rp.role_id = #{roleId}
    </select>
</mapper>
=======
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.springcloudprovider8081.dao.PermissionMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.example.springcloudprovider8081.entity.po.PermissionPO">
        <id column="id" property="id" />
        <result column="label" property="label" />
        <result column="parent_id" property="parentId" />
        <result column="parent_name" property="parentName" />
        <result column="code" property="code" />
        <result column="path" property="path" />
        <result column="name" property="name" />
        <result column="url" property="url" />
        <result column="type" property="type" />
        <result column="icon" property="icon" />
        <result column="create_time" property="createTime" />
        <result column="update_time" property="updateTime" />
        <result column="remark" property="remark" />
        <result column="order_num" property="orderNum" />
        <result column="is_delete" property="isDelete" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, label, parent_id, parent_name, code, path, name, url, type, icon, create_time, update_time, remark, order_num, is_delete
    </sql>

    <!--根据用户ID查找权限列表-->
    <select id="findPermissionListByUserId" resultMap="BaseResultMap">
        select
            DISTINCT
            p.id, p.parent_id, p.label, p.`code`, p.url, p.type, p.icon, p.remark, p.path, p.name
        from
            sys_user as u
        left join sys_user_role as ur on u.id = ur.user_id
        left join sys_role as r on ur.role_id = r.id
        left join sys_role_permission as rp on rp.role_id = r.id
        left join sys_permission as p on rp.permission_id = p.id
        where
            u.id = #{userId}
        order by p.id asc
    </select>

    <select id="findPermissionListByRoleId" resultMap="BaseResultMap">
        select
            p.id, p.parent_id, p.label, p.parent_name, p.`code`, p.url, p.type, p.icon, p.remark, p.path, p.name,
            p.create_time, p.update_time, p.order_num, p.is_delete
        from
            sys_permission as p
        inner join sys_role_permission as rp on p.id = rp.permission_id
        where
            rp.role_id = #{roleId}
    </select>
</mapper>
>>>>>>> 437f1fc (注册中心集群 & 基本内容 & OpenFeign负载均衡 & Hystrix熔断机制(自编写Hystrix熔断策略防止ThreadPool无法拿到Request))
